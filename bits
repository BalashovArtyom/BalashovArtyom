#include <stdio.h>

void main() {
    // 1
    /*
    int n;
    scanf("%d", &n);
    if (n == 0)
        printf("2 в степени %d = %d", n, 2 >> 1);
    if (n>0)
        printf("2 в степени %d = %d", n, 2 << n-1);
    */
    
    
    // 2, 3, 4
    /*
    int i, N, k, high_bit;
    N = 128;
    i = 65536;
    k = 0;
    high_bit = 0;
    while (1>0) {
        if (N & i){        
            printf("1");
            k++;
        }
        else
            printf("0");
        if (k>0)    
            high_bit++;
        if (i == 1)       
            break;
        i >>= 1;         
    }  
    printf("\nКол-во единиц = %d\n", k);
    printf("Старший бит стоит на позиции %d\n", high_bit-1); 
    //Как я понял, старший бит это первая единица со старшим коэффициентом
    //Есди старшим коэффициентом является первый 0 после единицы со старшим коэффициентом,
    //то в принте нужно указать high_bit вместо high_bit-1
    */
    
    // 12
    // выведет 0, если x - степень двойки, так степень двойки имеет представление вида 0...010...0
    //                                              тогда x-1 имеет представление вида 0...001...1
    //иначе выведется число меньше или равное числу x-1
    
}
