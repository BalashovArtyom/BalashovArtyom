Задача N1
#include <stdio.h>
#include <math.h>
int main()
{
    int a, b, c;
    double x, x1, x2, D;
    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);
    D=b*b -4*a*c;
    if(D>=0)
    {
        x1=(-b-sqrt(D))/(2*a);
        x2=(-b+sqrt(D))/(2*a);
        printf("x1=%f\n", x1);
        printf("x2=%f", x2);
    }
    else
    {
        printf("корней нет");
    }
    return 0;
}
ПРИМЕР 1)
1                                                                                                                              
2                                                                                                                              
10                                                                                                                             
корней нет
ПРИМЕР 2)
2
2                                                                                                                              
-5                                                                                                                             
-12                                                                                                                            
x1=-1.500000                                                                                                                   
x2=4.0000
