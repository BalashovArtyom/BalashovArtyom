import numpy as np
import matplotlib.pyplot as plt
y = lambda x: 0.3*np.sin(2*x)
fig = plt.subplots(figsize=(10,3))
x = np.linspace(-5, 5, 100)
plt.plot(x, y(x))
plt.title("График")
plt.xlabel("Ось X")
plt.ylabel("Ось Y")
plt.grid(True)
